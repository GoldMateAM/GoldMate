"use client";import Link from "next/link";import {Product} from "@/lib/types";import {useApp} from "./providers";
export function ProductCard({p}:{p:Product}){const {locale,t,money}=useApp();return <Link href={`/product/${p.id}`} className="product-card"><div className="product-media">{p.images?.[0]?<img src={p.images[0]} alt={p.title[locale]}/>:<div className="product-placeholder">GM</div>}{p.featured&&<span className="badge">{t.premium}</span>}</div><div className="product-info"><div className="product-meta">{p.purity} · {p.weightGrams}g · {t[p.color as keyof typeof t] as string}</div><h3>{p.title[locale]}</h3><div className="product-price">{money(p.priceAMD)}</div></div></Link>}
