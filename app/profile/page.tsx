"use client";import {useEffect,useState} from "react";import {collection,doc,onSnapshot,orderBy,query,setDoc,where} from "firebase/firestore";import {getDownloadURL,ref,uploadBytes} from "firebase/storage";import {db,storage} from "@/lib/firebase";import {useApp} from "@/components/providers";import {toast} from "sonner";import {AMD} from "@/lib/utils";
export default function Profile(){const {profile,loading,t,google,login,register,logout,refresh}=useApp(),[mode,setMode]=useState<"login"|"register">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[name,setName]=useState(""),[phone,setPhone]=useState(""),[orders,setOrders]=useState<any[]>([]),[file,setFile]=useState<File|null>(null);useEffect(()=>{if(profile){setName(profile.displayName||"");setPhone(profile.phone||"");const q=query(collection(db,"orders"),where("buyerId","==",profile.id),orderBy("createdAt","desc"));return onSnapshot(q,s=>setOrders(s.docs.map(d=>({id:d.id,...d.data()}))))}},[profile?.id]);if(loading)return <main className="shell page">{t.loading}</main>;if(!profile){async function submit(){try{mode==="login"?await login(email,password):await register(name,email,password)}catch(e:any){toast.error(e.message)}}return <main className="shell page"><div className="panel" style={{maxWidth:500,margin:"40px auto"}}><div className="eyebrow">GoldMate</div><h1 style={{fontFamily:"Georgia",fontWeight:500,fontSize:40}}>{mode==="login"?t.signin:t.register}</h1><div style={{display:"grid",gap:10}}>{mode==="register"&&<input className="field" placeholder={t.name} value={name} onChange={e=>setName(e.target.value)}/>}<input className="field" placeholder={t.email} value={email} onChange={e=>setEmail(e.target.value)}/><input className="field" type="password" placeholder={t.password} value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn-primary" onClick={submit}>{mode==="login"?t.signin:t.createAccount}</button><button className="btn-secondary" onClick={()=>google().catch(e=>toast.error(e.message))}>{t.google}</button><button className="btn-secondary" onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?t.noAccount:t.haveAccount}</button></div></div></main>}
async function save(){try{let photoURL=profile.photoURL||"";if(file){const s=ref(storage,`public/${profile.id}/profile-${Date.now()}-${file.name}`);await uploadBytes(s,file);photoURL=await getDownloadURL(s)}await setDoc(doc(db,"users",profile.id),{displayName:name,phone,photoURL},{merge:true});await refresh();toast.success(t.success)}catch(e:any){toast.error(e.message)}}return <main className="shell page"><div className="page-title"><div className="eyebrow">GoldMate</div><h1>{t.profileTitle}</h1></div><div className="profile-grid"><div className="panel profile-card"><img className="avatar" src={profile.photoURL||"/brand/goldmate-logo.png"}/><h2>{profile.displayName}</h2><p>{profile.email}</p><div style={{marginTop:18}}>{profile.sellerApproved?<span className="verified">✓ {t.sellerApproved}</span>:<a className="btn-secondary" href="/seller/apply">{t.sellerApply}</a>}</div><button className="btn-secondary" onClick={logout} style={{marginTop:12}}>{t.signout}</button></div><div><div className="panel"><h2>{t.editProfile}</h2><div className="form-grid"><label><span className="form-label">{t.name}</span><input className="field" value={name} onChange={e=>setName(e.target.value)}/></label><label><span className="form-label">{t.phone}</span><input className="field" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label className="full"><span className="form-label">{t.photos}</span><input className="field" type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/></label></div><button className="btn-primary" style={{marginTop:14}} onClick={save}>{t.save}</button></div><div className="panel" style={{marginTop:16}}><h2>{t.myOrders}</h2>{orders.length?orders.map(o=><div key={o.id} style={{padding:"14px 0",borderBottom:"1px solid var(--line)",display:"flex",justifyContent:"space-between"}}><div><strong>{o.productTitle}</strong><div className="muted">{o.status==="new"?t.orderNew:o.status==="confirmed"?t.orderConfirmed:o.status==="completed"?t.orderCompleted:t.orderCancelled}</div></div><div>{AMD.format(o.amountAMD||0)}</div></div>):<div className="empty">{t.empty}</div>}</div></div></div></main>}
